/// [for문]
/// (반복문 내 사용할 임시 변수 선언;
/// 반복문 종료조건;
/// 한 반복 구간이 끝날때마다 실행될 코드;)
/// { 반복 구간마다 실행할 코드; }
/// 으로 이루어져있는 반복문입니다.

main() {
  for (var i = 0; i < 3; i++) {
    print(i);
  }
  // 0
  // 1
  // 2
}
