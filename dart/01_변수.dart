/// Dart에서는 [var]을 통하여 변수를 선언할 수 있습니다.
/// 변수는 어떠한 값을 담고(쓰고), 읽을 수 있습니다.
/// 다트패드에 아래와 같이 작성하고, 실행해봅시다.
/// print 함수를 이용하여 변수에 저장된 값을 Console창에 나타나게 할 수 있습니다.

void main() {
  var color = "red";
  print(color);
}
